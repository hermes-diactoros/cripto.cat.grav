{% extends 'partials/base.html.twig' %}

{% block content %}
<section class="section">
<div class="container">
	<h1 class="title">{{ page.title }}</h1>
	<h2 class="subtitle">{{ page.date|date }}—<a href="mailto:{{ page.header.author.email }}">{{ page.header.author.name }}</a></h2>
	<hr>
	<div class="content">
		{{ page.content }}
	</div>
</div>
</section>
{% endblock %}
